function conf_del(dtfile,etn)
{
	 var r=confirm("Are you sure you want to delete "+dtfile+"."+etn);
	 if (r==true)
	 {
	  window.location = "del_code.php?dfile="+dtfile;
	 }	 
	 else
	 {
	  location.reload();
	 }
}	
function change_lang(nlan)
{
	window.location = "load_dir.php?alang="+nlan;
}
